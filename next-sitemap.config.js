/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://www.erwinwijnveld.nl',
    generateRobotsTxt: true, // (optional)
    // ...other options
}